%h1 messages index

%p= link_to "create a new message", new_message_path

%table.table

  %thead
    %tr
      %th Date
      %th User
      %th Content

  %tbody
    - @messages.each do |message|
      %tr
        %td= l message.created_at
        %td= message.user.email
        %td= message.content

%p= link_to "create a new message", new_message_path
