%input{:id => "team_id", :value => @team.id, :type => "hidden"}

.giant-new-message
  = simple_form_for [ @team, @new_message ] do |f|
    = f.input_field :content, :as => :string, :placeholder => "Update your status here ..."

= present @team do |team_presenter|

  #team_badges= team_presenter.badges
